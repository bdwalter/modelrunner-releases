# ModelRunner Releases

This repository hosts release artifacts and the Sparkle appcast for
[ModelRunner](https://github.com/bdwalter/ModelRunner).

`appcast.xml` is consumed by Sparkle in the shipped app to discover and
verify updates (EdDSA-signed). DMGs are attached to GitHub releases on this
repo; `appcast.xml` is updated as part of each release.
